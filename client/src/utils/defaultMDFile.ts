import { MDFile } from "./types";

const defaultMDFile: MDFile = {
    name: 'Untitled',
    author: '',
    _id: '',
    path: '',
    lastModified: new Date(Date.now())
}

export default defaultMDFile;